file := 'testdata/basic.json'
jsonl_file := 'testdata/events.jsonl'
session := 'a555less-e2e'
width := '100'
height := '30'

_help:
    @just --list --list-heading $'Make sure you build the binary first\n\ne2e:\n'

# Run e2e test suite.
run: \
    collapse \
    help-expanded \
    help-simple \
    navigation \
    search \
    smoke \
    smoke-jsonl

# Test.
collapse:
    ./test_collapse.sh "{{file}}" "{{session}}" "{{width}}" "{{height}}"

# Test expanded help.
help-expanded:
    ./test_help_expanded.sh "{{file}}" "{{session}}" "{{width}}" "{{height}}"

# Test simple help.
help-simple:
    ./test_help_simple.sh "{{file}}" "{{session}}" "{{width}}" "{{height}}"

# Test.
navigation:
    ./test_navigation.sh "{{file}}" "{{session}}" "{{width}}" "{{height}}"

# Test search and match navigation.
search:
    ./test_search.sh "{{file}}" "{{session}}" "{{width}}" "{{height}}"

# Test.
smoke:
    ./test_smoke.sh "{{file}}" "{{session}}" "{{width}}" "{{height}}"

# Test JSONL rendering.
smoke-jsonl:
    ./test_smoke_jsonl.sh "{{jsonl_file}}" "{{session}}" "{{width}}" "{{height}}"
